# Render Audit: dgist-2026f-w02

**Date:** 2026-08-30
**Profile:** lecture
**Audience:** 93 first- and second-year undergraduates outside AI-related majors
**Inspected:** all 63 PNG captures in `/tmp/dgist-2026f-w02-shots`, covering 47 Reveal pages and every captured fragment state at 1280×720. The deck was not edited or rerendered.

## Summary

| Severity | Count |
|---|---:|
| Critical | 3 |
| Medium | 2 |
| Low | 2 |

The three critical findings are visible-content overflow failures: the final teleoperation reveal loses its video caption, the final GPT reveal loses its closing sentence, and the Two Waves slide loses both lower captions and its fragment keybox. All captured video slides and cards show real frames rather than black or missing posters. No capture shows raw HTML, a missing figure, an unintended empty slide, or a slide escaping the deck theme.

## Findings

| Screenshot / Reveal state | Slide | Severity | Issue | Recommendation |
|---|---|---|---|---|
| `12` — `h=8, v=0, f=3` | **Autonomous or teleoperated?** | **Critical** | The final video card runs through the bottom canvas edge. Its caption and metadata strip, including the essential `teleoperated` label, are entirely off-screen. The Atlas attribution footnote is also covered and truncated by the video. | Reduce the video-card height and reserve a fixed caption/footer band, or move the video beside the three reveal lines. Recheck the fully revealed `f=3` state, not only the initial slide. |
| `23` — `h=15, v=0, f=3` | **2018-2020 · GPT: One Simple Game** | **Critical** | The final line, `Fluent is not the same as true - keep both in mind.`, begins below the bottom edge and is clipped. The complete final narrative beat is therefore unavailable to the audience. | Shorten vertical gaps or the highlightbox, or shorten the quoted continuation. Keep the closing line fully above the footer in the final fragment state. |
| `30` — `h=21, v=0, f=-1`; `31` — `h=21, v=0, f=0` | **Two Waves, One Wall** | **Critical** | Both lower-image captions are clipped by the bottom edge and collide with the sources footer. In `f=0`, the keybox fragment is marked visible by Reveal but is completely below the canvas, so the slide's conclusion never appears. | Split the comparison across two slides or substantially reduce image/caption height. Keep the two lower captions, one-line sources footer, and fragment keybox in a measured content area above the slide controls. |
| `17` — `h=13, v=0, f=-1` | **2012 · AlexNet: Machines Learn to See** | **Medium** | The chart's axis labels, year labels, human-baseline label, and AlexNet annotation are too small for the back of a 93-person room, even though they are technically present and unclipped. These labels carry the quantitative argument. | Enlarge the chart materially, simplify its labels, and reclaim space from the three bullets if needed. Preserve the 2012 annotation and human baseline at room-readable size. |
| `49` — `h=37, v=0, f=-1` | **The pull now comes from industry, not academia** | **Medium** | The single-line sources footer extends into the slide-number area; its final source is visually merged with `38 / 47` and partially obscured. | Shorten the source labels or wrap them into a dedicated footer block that ends before the slide-number safe area. |
| `35` — `h=24, v=0, f=-1` | **Same recipe, new output** | Low | The rail's `INPUT`/`OUTPUT` micro-labels and subordinate phrases are too small to read from the room, although the larger bullets repeat the main idea. | Remove the micro-labels or raise all rail text to a room-readable size; keep only the three model names and the three input/output phrases. |
| `55` — `h=42, v=0, f=-1`; `56` — `h=43, v=0, f=-1`; `57` — `h=44, v=0, f=-1` | **What each week adds (1/3-3/3)** | Low | Several teaser images rely on embedded text and fine detail that is unreadable at their rendered thumbnail sizes, especially DreamZero, Open X-Embodiment, Diffusion Policy, VGGT, and the safety-report cover. The surrounding session copy remains readable. | Use tighter hero crops with one recognizable visual subject, or remove embedded document text and rely on the large session labels. |

## Checks Passed

- Every video-bearing capture inspected (`07`, `12`, `29`, `37`, `42`-`47`, and `50`) contains a valid frame; none is black, empty, or missing its poster. The Atlas capture at `46` is letterboxed but valid.
- All shared series slides render in the clean-academic theme. The Wooclap fallback cards at `06` and `62` visibly show `code announced in class` and are not broken QR placeholders.
- The sparse initial states at `08` and `58` are intentional pre-fragment states, not empty-slide failures.
- No capture contains raw HTML, a broken shortcode, a missing raster/SVG, unintended overlap outside the findings above, or low-contrast text that disappears into its background.
