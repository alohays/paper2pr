# TTA 2026 Cosmos 3 Demo Embed Plan

**Status:** COMPLETED
**Date:** 2026-08-31
**Deck:** `Quarto/talks/tta-2026.qmd`
**Target slide:** `Cosmos 3: one omnimodal world model`

## Communication Job

By the end of the slide, standards practitioners should connect the Cosmos 3 model claim with one representative official demo while retaining the adoption and claim-scope context.

## Requirements

- Use the existing official-source Cosmos media entry if its provenance and content match the Cosmos 3 claim; otherwise add a properly sourced manifest entry.
- Preserve the demo's original aspect ratio and visible frame without cropping.
- Keep the video compact and secondary to the slide's model claim.
- Prevent overlap with the title, claim chips, adopter text, and source footnote.
- Do not reduce the deck's root type size or use forbidden small-text escape hatches.
- Preserve the Korean speaker notes as local-only content.

## Execution

1. Inspect the current slide, existing Cosmos manifest entry, and rendered frame.
2. Select the smallest layout change that creates a dedicated uncropped media frame.
3. Edit the QMD and, only if required, the video manifest/lock.
4. Render the deck and capture the target slide at 1280 by 720.
5. Inspect for clipping, overlap, legibility, and source attribution.
6. Run the deck quality gate and public-form note stripping check.

## Verification

- Target slide renders at 1280 by 720 with no clipped content.
- The embedded video uses `object-fit: contain` or the manifest card's uncropped default.
- Title, adoption context, and source line remain readable.
- Quality score remains at least 90, with no blocker.
- Local HTML keeps speaker notes; stripped preview contains none.

## Outcome

- Added the untrimmed official NVIDIA Cosmos 3 `reason2gen-final.mp4` asset as `cosmos3-reason2gen`.
- Verified source bytes at SHA-256 `421626fca122c9fe069c75abb98c79b659cb55aee833fe048f640d12d27cb84a`.
- Rendered the target as a two-column layout with a 440 by 247.5 pixel video frame using `object-fit: contain`.
- Measured 66 pixels between the title and body and 39 pixels between the body and footnote, with no overflow.
- Published the MP4 and poster to `media-tta-2026`; all 68 release assets return HTTP 200.
- Passed the 100/100 quality gate, the complete media regression suite, and the production-equivalent site assembly.
- The stripped preview contains 47 sections, one Cosmos 3 card, zero note payloads, and zero local-media references.
